import React, { Component } from 'react'
import { connect } from 'react-redux'

class LoanDetail extends Component {
    render() {
        return (
            <div>Welcome</div>
        )
    }
}
const mapState = (state) => ({

})
const mapProps = (dispatch) => {
    return {

    }
}
export default connect(mapState, mapProps)(LoanDetail)